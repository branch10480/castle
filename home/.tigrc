# 未ステージ差分を “まとめて” Kaleidoscope で開く（File list/left pane）
bind status  K  @sh -c 'cd "$(git rev-parse --show-toplevel)" && git difftool -y -t Kaleidoscope'

# ステージ済みのファイルについて
bind status  S  @sh -c 'cd "$(git rev-parse --show-toplevel)" && git difftool -y -t Kaleidoscope --staged'
